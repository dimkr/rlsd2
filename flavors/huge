set repo file:///repo
set curl [curl]
set available [packlim::available $curl $repo ""]
set packages [dict keys [lindex $available 0]]

foreach package $packages {
	packlim::install $curl $repo {*}$available $package locked ""
	packlim::lock $package
}

packlim::purge
